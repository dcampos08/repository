library( civis.r.client )

write_civis(
  df=dataframe,
  tablename='schema.new_table',
  database='database',
  distkey='id',
  sortkey1='id'
  sql=paste('select * from schema.old_table where id = x' )
)
